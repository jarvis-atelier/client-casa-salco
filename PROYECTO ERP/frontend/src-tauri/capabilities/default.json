{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities por defecto para la ventana principal",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "window-state:default"
  ]
}
